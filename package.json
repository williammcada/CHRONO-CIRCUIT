{
  "name": "chrono-circuit",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "An eight-stage action adventure with modular math practice",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=22" }
}
