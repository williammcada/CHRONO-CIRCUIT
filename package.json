{
  "name": "chrono-circuit",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "An eight-stage elapsed-time action adventure",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=22" }
}
